import { HideImgDirective } from './hide-img.directive';

describe('HideImgDirective', () => {
  it('should create an instance', () => {
    const directive = new HideImgDirective();
    expect(directive).toBeTruthy();
  });
});
